# crimon で作業するエージェントへ

このファイルは **Codex / GPT が自動で読む場所**です。
Claude は `CLAUDE.md` を読みます。**中身は同じ約束**にしてあります。
片方だけ直すと、もう片方が古い決まりのまま作業します。**両方を直すこと。**

サマナーズウォー風の4対4ターン制モンスターRPG。TypeScript + Vite のPWA。

---

## 依頼主との約束(必ず守ること)

- **応答・報告・コミットメッセージはすべて日本語で書く。** 3回叱られている。
  毎回の犯人は同じ2つで、**どちらも画面に出るのに見落としやすい**:
  1. **思考ブロック**。ここが英語だと、本文が日本語でも画面は英語で埋まる
  2. **作業一覧の項目名**。一度書くと残り続ける
- **mainへ毎回反映する。確認を取らない。** ブランチ → PR → squashマージまで通す
- **安っぽいBGM・効果音は絶対に入れない**
- **確率の数字を画面やREADMEに出さない。** ガチャの排出率もドロップ率もコード内だけ。
  ただし**戦闘スキルの発動%(「65%で火傷」など)は出してよい**。これは
  プレイヤーが編成を考えるために要る情報なので、伏せる対象ではない

## 芯にある考え方

`docs/design-concept.md` を読むこと。要点は2つ。

- **ふつうのモンスターでも、育てて装備を整えれば奥まで行ける**
- **スキルがモンスターに、いろんな場所での役割を与える**

難易度を上げる時、この2つを潰していないかを必ず確かめる。
一度「毒と耐久で抜けられるのは抜け道だ」と考えてボスに継続ダメージ耐性を
持たせたが、**毒も耐久もちゃんとした戦術**であって、それは戦い方を選んだこと
自体への罰だった。依頼主の指摘で撤回している。

---

## この案件で繰り返し痛い目を見たこと

### 型チェックとテストは、CSSとWebGLの安全弁にならない

**これがこの案件で最も重要な一文です。**

GLSLのコンパイル失敗も、重なり順の誤りも、`position` の指定漏れも、
**クラス名のずれ**も、**ボタンが他の要素に覆われて押せないこと**も、全部素通りします。

実際に出した事故:

- 接地影が乗算合成で効かず、足元に**白い矩形**が4枚並んだ
- CSSが `arena-tickets__pip` / markupが `arena-ticket__pip` で、部品が1つも出なかった
- HPが5桁で札からはみ出し、`16317/23440` が `6317/2344` に化けていた
- ★6が★★★★に切り詰められ、**星が2つ少なく見えていた**
- 設定シートの一番下のボタンが、下タブに覆われて**押せなかった**
- 初心者ミッションの浮遊パネルがモンスター画面の上部を覆い、
  **絞り込みと並べ替えが押せなかった**(型もテストも全部通っていた)

**必ず実ブラウザで開いて、自分の目で画像を見ること。**
そのうえで、機械で拾えるものは機械に拾わせること(次節)。

### 押せるかどうかを `.click()` で判定しない

プログラムからの `.click()` は、**要素が他のものに覆われていても成功します。**
必ず `document.elementFromPoint` で「その座標にいる最前面が自分か」を見ること。

### 測ってから判断する

- **勝率だけで難易度を比べない。** 上げすぎると全編成そろって0%に張り付き、
  何も読めなくなる。決着時点の敵残HP割合など、飽和しない値で見る
- **その戦術を実行できる顔ぶれか、先に確かめる。** 毒を1つも持たない3体を
  「毒編成」として測り、まるごと嘘の結論を出したことがある
- **直した本人は必ず「良くなった」と言う。** 前後を左右伏せて並べる
  `tools/blindCompare.mjs` で別の目に判定させる

### 片方だけ触らない

装備の速度を半分にした時、敵の速度カーブを据え置いたら装備ダンジョンが
8階44% / 9階4% / 10階0% まで崩れた。**プレイヤーの上限と敵のカーブは対で決まる。**

### 数値は「引いた時に確定して控えに焼かれる」

装備の生成側を変えても、既に持っている装備には効かない。
バランスを変える時は**控えの移行**も一緒に入れること。入れないと、
前から遊んでいる人だけが古い性能を持ち続ける。
同じ理由で、**保存の項目名を変える時は必ず読み替えを書く**
(レベル上げダンジョンを3段階から5階へ変えた時に実施済み)。

### 手数をまとめるのと、戦闘を飛ばすのは別のこと

周回を「戦闘を実行せずに決着だけ出す」作りにしていたため、
**戦闘画面を一度も見ないまま遊べていた**。依頼主の指摘で1戦ずつ実際に戦う形へ変えた。
まとめてよいのは**押す手数**であって、戦闘そのものではない。

---

## 作業の進め方

```
npx tsc --noEmit                       # 型
npx vitest run                         # テスト
node tools/harness.mjs &               # 常駐サーバ(実ブラウザ)
HARNESS_PORT=<port> node tools/tour.mjs    # 全画面の巡回 ← 最後に必ず通す
npm run scene -- --list                # 1画面を狙った状態で開く
npm run monster                        # モンスターのデータ検診
```

### 巡回(tour)は、この案件で唯一の安全網です

はみ出し・**押せないボタン**・小さすぎる文字・重なりを機械的に拾います。
PRでも自動で走ります(`.github/workflows/checks.yml`)。

**画面を作り替えた時は、`data-tour` の目印を新しいボタンへ必ず引き継ぐこと。**
目印が外れると巡回がその画面へ辿り着けず、**安全網が黙って外れます。**
実際にホームの作り替えでこれが起き、28画面のうち22画面が検査されないまま、
押せないボタンが複数そのまま出ました。

- タブ: `data-tour="tab:<ScreenName>"`
- 一覧の枠: `data-tour="tile:<name>"`
- タイトルのSTART: `data-tour="start"`

CIは目印の数も見ています。減っていると落ちます。

**覆われた要素も `querySelector` と `.click()` では掴めます。**
そのせいで巡回はタイトル画面の裏にあるタブを押し、「ホーム = 問題なし」と
報告しながら、実際にはタイトル画面を検査していました。ホームは巡回の一番目なので、
**丸ごと素通りしていた。** いまは `data-tour="start"` を必ず押してから検査します。

### 浮かせた部品は、必ず下の何かを覆う

この案件で同じ事故を3回出しています。

- 初心者ミッションの浮遊パネル → モンスターの絞り込み・並べ替えが押せない
- ホームの初心者ミッション(`position:fixed; left:94px; top:164px`)
  → 横向きで図鑑・ダンジョンが押せない
- ログインボーナスの札(`position:absolute; top:78px`)
  → 2枚重なると下の札の「閉じる」が押せない

**案内・進捗・お知らせの類は、画面の流れの中に置くこと。**
ドラッグで避けられるのは「気づいた人」だけで、案内が要る人ほど気づけません。
共通部品は `src/web/ui/tutorialBar.css` の `.tutorial-bar` があります。

### 潰れた箱は、中身を黙って切り落とす

ホームは `height:100dvh` の flex 縦並びです。入りきらないと**世界の枠だけが潰れ**、
`overflow:hidden` が中の「試練の塔」を切り落として、押せないボタンになりました。
高さを削り合う配置では、**潰さずに巻物を出す**方を選ぶこと。

### CSSを足す時

`style.css` へ追記し続けると **Viteが黙って空のCSSを配るようになります。**
画面が崩れたら、まず配信バイト数を見ること(`docs/dev-tools.md`)。
新しい画面のCSSは `src/web/ui/<画面名>.css` に分けて書くこと。

---

## 複数のエージェントで進める時

いま **Claude と GPT(Codex)の両方**がこのリポジトリで作業します。
話す口はお互いにありません。**リポジトリだけが共有の通り道**です。
だから、口約束ではなく**ファイルと自動検査**に決まりを持たせること。

- 担当ファイルは**重ならないよう割り当てる**(同じファイルを複数が触って作業が混ざった)
- `git add` は担当ファイルだけを明示指定。**`git add -A` と `git add .` は禁止**
- **一時ファイルをコミットしない**(`temp` / `__dummy__` を実際にmainへ入れた事故がある)
- コミットメッセージは日本語で、**なぜそうしたか**を書く。何をしたかは差分を見れば分かる
- 決まりを変えたら **`AGENTS.md` と `CLAUDE.md` の両方**を直す

### いま進んでいる大きな方針

**モンスターの造形を3Dから2Dへ移す**方向で検討中(15種 × 6属性)。

理由は、3D関連がソースの44%(19,466行)を占めながら、実機で31.5fpsしか出ておらず、
見た目の採点でも造形が第1位の課題のまま動いていないため。
そして**軽くするにも見た目を上げるにも、どのみち造形の作り直しが要る**と分かっている。

**いきなり3Dを消さないこと。** 2〜3体を実際の戦闘画面に置いて、
フレームレートと見た目を測ってから全体を進める。
