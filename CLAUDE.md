# クリエイトモンスターズ (crimon)

サマナーズウォー風の4対4ターン制モンスターRPG。TypeScript + Vite のPWAで、
戦闘の描画は Three.js。

## 依頼主との約束(必ず守ること)

- **応答はすべて日本語で書く。** 英語に戻ってしまい、3回叱られている。
  毎回の犯人は同じ2つで、**どちらも画面に出るのに見落としやすい**:
  1. **思考ブロック**。ここが英語だと、本文が日本語でも画面は英語で埋まる。
     いちばん多い漏れ方がこれ
  2. **作業一覧(TaskCreate)の項目名**。一度書くと残り続ける
- **mainへ毎回反映する。確認を取らない。** ブランチ → PR → squashマージまで通す
- **プレイヤー向けの変更を反映するたび、HOMEのアップデート情報へ必ず追記する。**
  `src/game/compensation.ts` の先頭へ、変更内容が分かる `kind: "UPDATE"` のお知らせを追加する。
  確率の数値は書かず、プレイヤーが実際に確認できる変更点を漏らさないこと
- **安っぽいBGM・効果音は絶対に入れない**
- **確率の数字を画面やREADMEに出さない。** ガチャの排出率もドロップ率もコード内だけ。
  ただし**戦闘スキルの発動%(「65%で火傷」など)は出してよい**。これは
  プレイヤーが編成を考えるために要る情報なので、伏せる対象ではない
- 作業ブランチは `claude/turn-based-battle-game-ioqjai`

## 芯にある考え方

`docs/design-concept.md` を読むこと。要点は2つ。

- **ふつうのモンスターでも、育てて装備を整えれば奥まで行ける**
- **スキルがモンスターに、いろんな場所での役割を与える**

難易度を上げる時、この2つを潰していないかを必ず確かめる。
一度「毒と耐久で抜けられるのは抜け道だ」と考えてボスに継続ダメージ耐性を
持たせたが、**毒も耐久もちゃんとした戦術**であって、それは戦い方を選んだこと
自体への罰だった。依頼主の指摘で撤回している(`tests/continuousDamage.test.ts`)。

## この案件で繰り返し痛い目を見たこと

### 型チェックとテストは、CSSとWebGLの安全弁にならない

GLSLのコンパイル失敗も、重なり順の誤りも、`position` の指定漏れも、
**クラス名のずれ**も素通りする。実際に出した事故:

- 接地影が乗算合成で効かず、足元に**白い矩形**が4枚並んだ
- CSSが `arena-tickets__pip` / markupが `arena-ticket__pip` で、部品が1つも出なかった
- HPが5桁で札からはみ出し、`16317/23440` が `6317/2344` に化けていた
  (**現在値が最大値より大きい**という有り得ない表示)
- ホームの経験値が8px、試練の塔の関門名が7.4px。実機で読めない
  (いまは `tests/cssReadability.test.ts` が9px未満を落とす)

**必ず実ブラウザで開いて、自分の目で画像を見ること。**
道具は `docs/dev-tools.md`。

### 押せるかどうかを `.click()` で判定しない

プログラムからの `.click()` は、**覆われていても成功する。**
必ず `document.elementFromPoint` で最前面が自分かを見ること。
これを怠って、巡回がタイトル画面の裏のタブを押し、
**「ホーム = 問題なし」と報告しながらタイトル画面を検査していた**。
ホームは巡回の一番目なので、丸ごと素通りしていた
(いまは `data-tour="start"` を必ず押す)。

### 浮かせた部品は、必ず下の何かを覆う

同じ事故を3回出している。初心者ミッションの浮遊パネル、
ホームの `position:fixed` の小窓、ログインボーナスの `position:absolute` の札。
どれも押せないボタンを作った。**案内・進捗・お知らせは画面の流れの中に置く。**
共通部品は `.tutorial-bar`(`src/web/ui/tutorialBar.css`)。

### 潰れた箱は、中身を黙って切り落とす

ホームは `height:100dvh` の flex 縦並び。入りきらないと世界の枠だけが潰れ、
`overflow:hidden` が「試練の塔」を切り落として押せなくしていた。
高さを削り合う配置では、**潰さずに巻物を出す**方を選ぶこと。

### 測ってから判断する

- **勝率だけで難易度を比べない。** 上げすぎると全編成そろって0%に張り付き、
  何も読めなくなる。決着時点の敵残HP割合など、飽和しない値で見る
- **その戦術を実行できる顔ぶれか、先に確かめる。** 毒を1つも持たない3体を
  「毒編成」として測り、まるごと嘘の結論を出したことがある
- **直した本人は必ず「良くなった」と言う。** 前後を左右伏せて並べる
  `tools/blindCompare.mjs` で別の目に判定させる

### 片方だけ触らない

装備の速度を半分にした時、敵の速度カーブを据え置いたら装備ダンジョンが
8階44% / 9階4% / 10階0% まで崩れた。**プレイヤーの上限と敵のカーブは対で決まる**
(`tests/speedBudget.test.ts` が両者の比を見ている)。

### 手数をまとめるのと、戦闘を飛ばすのは別のこと

周回(10回まとめて挑む)を「戦闘を実行せずに決着だけ出す」作りにしていたため、
**戦闘画面を一度も見ないまま遊べていた**。依頼主の指摘で1戦ずつ実際に戦う形へ変えた。
まとめてよいのは**押す手数**であって、戦闘そのものではない。

代わりに再生速度が効く。実測(装備ダンジョン5階)で1戦あたり x8 が約37秒、
等倍はその8倍。だから速度は保存して起動をまたいで残す。

### 数値は「引いた時に確定して控えに焼かれる」

装備の生成側を変えても、既に持っている装備には効かない。
バランスを変える時は**控えの移行**も一緒に入れること。入れないと、
前から遊んでいる人だけが古い性能を持ち続ける。

## 作業の進め方

```
npx tsc --noEmit                  # 型
npx vitest run                    # テスト(551件)
node tools/harness.mjs &          # 常駐サーバ(実ブラウザ)
HARNESS_PORT=<port> node tools/tour.mjs   # 全28画面の巡回
```

**巡回は最後に必ず通す。** はみ出し・押せないボタン・9px未満の文字を機械的に拾う。

`style.css` へ追記し続けると **Viteが黙って空のCSSを配るようになる**。
画面が崩れたら、まず配信バイト数を見ること(`docs/dev-tools.md`)。

## エージェントを立てる時の規則

- 担当ファイルは**重ならないよう割り当てる**(同じファイルを複数が触って作業が混ざった)
- `git add` は担当ファイルだけを明示指定。**`git add -A` と `git add .` は禁止**
- **`git push` はしない**(取りまとめ役がまとめて行う)
- **`pkill -f harness.mjs` は絶対に使わない。** 他の担当のサーバまで殺す。
  止めるのは自分のポートだけ(`lsof -ti :<port> | xargs -r kill`)
- 報告は日本語
- **同時に立てるのは2〜3個まで。** 5個並べてセッション上限を焼き切った

## 今どこまで進んでいるか

作ってあるもの: ステージ(4章)/ 装備・育成・ゴールドの各ダンジョン / 召喚 /
クリエイト(スキル移し替え)/ アリーナ(対人戦)/ **試練の塔** / 図鑑 / ショップ / 遊び方

**試練の塔**は、既存のダンジョンが1階=1戦で毎回全回復から始まるせいで
回復・継続回復・免疫・耐久に出番が無かった、という穴を埋めるために足した。
HPとクールタイムを次の階へ持ち越し、倒れた仲間は節(10階ごと)まで戻らない。
実測(★6Lv60+★5装備・30回ずつ)で 通常25階 / 高レア28階 / 耐久25階 / 毒18階。
**通常編成だけで20階を越えられる**ことが、この塔がコンセプト1を守っている証拠。
数値を触る時は `npx tsx tools/towerPressure.mjs` で測ってから。

**この数字は前は 23/26/24/16 と書いてあった。** 11種を入れた後に測り直したら
全部2階ほど上だったので、11種を入れる前のmain(`753d0d9^`)でも測ったところ
24/28/25/18 だった。**エンジンの変更ではなく、記録が古かっただけ。**
数字を疑ったら、まず前のコミットで測って切り分けること。

### 11種は、まだ塔の穴を埋められていない

解除と回復阻害はプレイヤー側に入った。**が、それで登れるようにはなっていない。**
アビスリーパー(解除)+マッシュルン(回復阻害)+ヴァルキリア+シェルタートルの
「新種」編成は **22階**で、通常編成(25階)より下。装備を極めても 27階 対 28階。

しかも**詰まった階は23階(30回中18回)で、ここは「守りの階」**——
敵が強化を張り直す、まさに解除で答えるはずの階。答えを持っていく編成が
その答えの階で止まっている。手が入っていないのは次のどれか:

- 解除1個では張り直しに追いつかない(数か、頻度か)
- 星3〜5混成の素の能力が、通常編成(ナイト・ウルフ・インプ・フェアリー)に届いていない
- 傾向そのものが解除で崩れる作りになっていない(`--traits` で確かめる)

`npx tsx tools/towerPressure.mjs --teams 新種` で再現できる。**測ってから触ること。**

### 塔の51階以降は、50階までと作りが違う

50階までは1本の等比曲線(`towerPowerOf`)が難易度そのもの。
**51〜99階の通常階はそこに乗っていない。**顔ぶれ・属性を1階ずつ手で書いた固定データ
(`src/data/trialTowerUpper.ts`)と、10階ぶんで約1.2倍しか伸ばさない実数の帯で組んである。
60階は専用編成(`src/data/trialTowerFloor60.ts`)。**70/80/90/100階だけが従来の曲線のまま。**

曲線を51階以降へ延ばさなかったのは、100階で1100(=50階の20倍)になり、
「育てば届く」ではなく別の単位の敵になるから。圧は数字ではなく**編成の質**で作る。

- 属性は種族と一緒に手で決める。**templateIdだけで決めると事故る**
  (火のゴーレムに盾は無い)。`tests/trialTowerUpper.test.ts` が
  階の狙いと実際のスキルを機械的に突き合わせている
- 実効値は `fixedStats` に実数で入る。倍率(powerScale/speedScale)は**掛からない**
- 「群れの階」は51階以降では配らない(全体攻撃の有無だけで決まる階になる)
- **1〜50階と70/80/90/100階はダイジェストで固定してある。**
  意図せず触ったら `tests/trialTowerUpper.test.ts` の「今回触っていないところ」が落ちる

本編の階をそのまま測るなら Battle Lab に `tower-f51`〜`tower-f99` が生えている
(`npx tsx tools/battleLab/index.ts --scenario tower-f99 --gear TYPICAL`)。
ただしこれは**全回復から1戦だけ**の値。持ち越しを含む到達階は towerPressure 側で測ること。

**追加した11種**(★3 マッシュルン/シェルタートル/コボルト、★4 バジリスク/ミミック/
ヴァルキリア/サンダービースト、★5 アビスリーパー/フェンリル/クロノス/ベヒモス)。
6属性ずつで66体、図鑑と召喚に載る。**星ごとの排出比率は触っていない**
(増えたのは「その星を引いた時の顔ぶれ」だけ)。

これで**塔の穴だった STRIP と HEAL_BLOCK がプレイヤー側にも入った**
(アビスリーパー・マッシュルン・ミミック・フェンリル)。
「癒やしの階」「守りの階」に答えができたので、**次は測り直し**
(`npx tsx tools/towerPressure.mjs`)。11種を入れた編成で階層が動くはず。

パッシブは `src/core/passive.ts`。**種類ごとの型**で書いてある(効果の配列ではない)。
Lv1〜5の値を5つ並べる方式で、アクティブの一律成長とは別物。
継承(クリエイト)の移し元には出さないが、**パッシブの枠を別のスキルへ変えるのは可能**。

見た目は `docs/visual-quality-rubric.md` の10項目で採点している。
**直近の採点は25/50で不合格。** 残っている最大の課題は次のとおり。

1. **モンスターの造形** — 古代のクリスタル系が「宙に浮いた宝石」にしか見えない。
   表面の素材差(鱗・毛・金属)が弱い
2. **エフェクトの情報量** — 属性ごとの形の差が乏しく、色違いに見える
3. **戦闘の重さ** — 実機の録画から実効31.5fpsと判明。描画回数は473→321まで
   減らしたが、本体だけで288ある。次に削るならモンスター1体あたりの部品数
   (ただし部位ごとに動かしているので、統合には造形の作り直しが要る)
